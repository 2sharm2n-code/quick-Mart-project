import { db, storage } from "hatchable";
export const access = "public";
export const methods = ["GET", "POST"];
const refPattern=/^QM-APP-[A-Z0-9]{10}$/;
const interviewQuestions=["Tell us briefly about yourself and the experience or skills that make you suitable for this role.","Why are you interested in this position, and what would you like to contribute to the team?","Tell us about a time you helped a customer, colleague, or another person solve a problem. What did you do?","How would you handle an angry or dissatisfied customer while remaining professional?","Imagine the branch is very busy and several customers need help at the same time. How would you prioritise your work?","Tell us about a time you worked as part of a team. What was your contribution?","What would you do if you made a mistake at work or noticed a mistake made by a colleague?","When would you be available to start work, and are you comfortable working shifts when the role requires it?"];
function cleanRef(v){return String(v||"").trim().toUpperCase();}
function publicStage(a){return {application_ref:a.application_ref,full_name:a.full_name,email:a.email,home_county:a.home_county,preferred_branch_county:a.preferred_branch_county,applied_position:a.applied_position,recruitment_stage:a.recruitment_stage,screening_decision:a.screening_decision,interview_date:a.interview_date,interview_time:a.interview_time,interview_invited_at:a.interview_invited_at,interview_expires_at:a.interview_expires_at,interview_submitted:!!a.interview_submitted_at,payment_status:a.payment_status,status:a.status};}
export default async function(req,res){const body=typeof req.body==="string"?JSON.parse(req.body||"{}"):req.body||{};const ref=cleanRef(req.method==="GET"?req.query?.ref:body?.applicationRef);if(!refPattern.test(ref))return res.status(400).json({success:false,message:"A valid QuickMart application reference is required."});const {rows}=await db.query("SELECT id, application_ref, full_name, email, home_county, preferred_branch_county, applied_position, education, highest_qualification, relevant_experience, motivation, recruitment_stage, screening_decision, interview_date, interview_time, interview_invited_at, interview_expires_at, interview_answers, interview_submitted_at, payment_status, status FROM applicants WHERE application_ref = $1 LIMIT 1",[ref]);const applicant=rows[0];if(!applicant)return res.status(404).json({success:false,message:"This recruitment link is no longer active. Please contact the recruitment team."});if(req.method==="GET"){const stage=String(applicant.recruitment_stage||"application_received");const interviewExpiresAt=applicant.interview_expires_at?new Date(applicant.interview_expires_at):null;const interviewWindowOpen=!!applicant.interview_invited_at&&!!interviewExpiresAt&&interviewExpiresAt.getTime()>Date.now();let next="screening";if((stage==="shortlisted"||stage==="interview")&&interviewWindowOpen)next="interview";else if((stage==="shortlisted"||stage==="interview")&&applicant.interview_invited_at)next="interview_expired";else if(stage==="shortlisted"||stage==="interview"||stage==="interview_submitted")next="waiting";else if(stage==="selected")next="careers";else if(["verification","onboarding","completed"].includes(stage))next="onboarding";else if(stage==="rejected")next="rejected";if(String(applicant.payment_status||"").toLowerCase()==="paid")next="completed";return res.json({success:true,stage:next,applicant:publicStage(applicant),questions:next==="interview"?interviewQuestions:[]});}
const stage=String(applicant.recruitment_stage||"");const interviewExpiresAt=applicant.interview_expires_at?new Date(applicant.interview_expires_at):null;const interviewWindowOpen=!!applicant.interview_invited_at&&!!interviewExpiresAt&&interviewExpiresAt.getTime()>Date.now();if(body?.mode==="onboarding_apply"){if(stage!=="selected")return res.status(403).json({success:false,message:"The careers application is only available after HR has selected this applicant."});const nationalId=String(body.nationalId||"").trim(),phone=String(body.phone||"").trim().replace(/[\s-]/g,""),position=String(body.appliedPosition||"").trim();const allowedPositions=new Set(["Store Assistant","Cashier","Branch Supervisor","Stock Controller","Cleaner","Security Guard","Shelf Attendant","Storekeeper","Customer Service Assistant","Driver","Warehouse Assistant","Security Supervisor"]);if(!/^\d{7,9}$/.test(nationalId))return res.status(400).json({success:false,message:"Enter a valid Kenyan National ID number (7–9 digits)."});if(!/^254(?:7|1)\d{8}$/.test(phone))return res.status(400).json({success:false,message:"Enter a valid Kenyan mobile number in 254 format."});if(!allowedPositions.has(position))return res.status(400).json({success:false,message:"Choose one of the available careers before continuing."});const {rows:updated}=await db.query("UPDATE applicants SET national_id = $2, phone = $3, applied_position = $4, recruitment_stage = 'onboarding' WHERE id = $1 AND recruitment_stage = 'selected' RETURNING application_ref, full_name, email, home_county, preferred_branch_county, applied_position, recruitment_stage, status",[applicant.id,nationalId,phone,position]);if(!updated.length)return res.status(409).json({success:false,message:"This application has already moved to another stage. Please reopen the official portal."});return res.json({success:true,message:"Career application confirmed. You may now continue to onboarding.",applicant:updated[0]});}
if(stage!=="shortlisted"&&stage!=="interview")return res.status(403).json({success:false,message:"The interview is not currently available for this application."});if(!applicant.interview_invited_at||!applicant.interview_expires_at)return res.status(403).json({success:false,message:"The interview invitation has not been issued yet. Please wait for the official invitation."});if(!interviewWindowOpen)return res.status(403).json({success:false,message:"The 24-hour interview access window has expired. The interview room is no longer available for this application."});const nationalId=String(body.nationalId||"").trim();if(!/^\d{7,9}$/.test(nationalId))return res.status(400).json({success:false,message:"Enter a valid Kenyan National ID number (7–9 digits) before submitting the interview."});if(applicant.national_id&&String(applicant.national_id)!==nationalId)return res.status(403).json({success:false,message:"The National ID does not match the ID already attached to this application."});let parsedAnswers=null;if(body?.answersJson){try{parsedAnswers=JSON.parse(String(body.answersJson));}catch{return res.status(400).json({success:false,message:"Interview answers could not be read. Please try again."});}}const answers=(parsedAnswers&&typeof parsedAnswers==="object"&&!Array.isArray(parsedAnswers))?parsedAnswers:((body?.answers&&typeof body.answers==="object"&&!Array.isArray(body.answers))?body.answers:body);if(!answers||typeof answers!=="object"||Array.isArray(answers))return res.status(400).json({success:false,message:"Please complete the interview questions."});const clean={};for(let i=0;i<interviewQuestions.length;i++){const value=String(answers[String(i)]??"").trim();if(value.length<10)return res.status(400).json({success:false,message:`Please answer interview question ${i+1} with at least 10 characters.`});clean[String(i)]=value.slice(0,2000);}const cvFile=Array.isArray(req.files)?req.files.find(f=>String(f.field||"")==="cv"):null;if(!cvFile||!cvFile.buffer||!cvFile.filename)return res.status(400).json({success:false,message:"Please attach your CV as a PDF, DOC, or DOCX file (maximum 5 MB)."});const allowedTypes=new Set(["application/pdf","application/msword","application/vnd.openxmlformats-officedocument.wordprocessingml.document"]);const filename=String(cvFile.filename).replace(/[^a-zA-Z0-9._-]/g,"_").slice(-120),lower=filename.toLowerCase();if(!allowedTypes.has(String(cvFile.contentType||""))&&!/[.](?:pdf|doc|docx)$/.test(lower))return res.status(400).json({success:false,message:"CV must be a PDF, DOC, or DOCX file."});if(Number(cvFile.buffer.length||0)>5*1024*1024)return res.status(400).json({success:false,message:"CV file must be 5 MB or smaller."});const cvUrl=await storage.put(`recruitment-cv/${applicant.id}/${crypto.randomUUID()}-${filename}`,cvFile.buffer,String(cvFile.contentType||"application/octet-stream"));const {rows:updated}=await db.query("UPDATE applicants SET national_id = $2, cv_url = $3, cv_filename = $4, interview_answers = $5::jsonb, interview_submitted_at = now(), recruitment_stage = 'interview_submitted' WHERE id = $1 RETURNING application_ref, full_name, applied_position, recruitment_stage, interview_submitted_at",[applicant.id,nationalId,cvUrl,filename,JSON.stringify(clean)]);return res.json({success:true,message:"Interview submitted successfully. The recruitment team will review your responses.",applicant:updated[0]});}